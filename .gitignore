*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
**/build
/.idese
/captures
.externalNativeBuild
.cxx
local.properties
/docs/superpowers
.kotlin
.superpowers/

# Build artifacts
shared/release/
androidApp/release/

# User-specific IDE files
.idea/deploymentTargetSelector.xml
.idea/deploymentTargetDropDown.xml
.idea/copilot.data.migration.ask2agent.xml

# Xcode user data (any project name)
**/xcuserdata/
*.xcuserstate

# Local Claude settings
.claude/settings.local.json

**/google-services.json
**/GoogleService-Info.plist
keystore.properties
*.jks
*.keystore

# Python caches
__pycache__/

# ASO screenshot working intermediates (keep only the approved set)
artifacts/aso-screenshots/*/
!artifacts/aso-screenshots/final/

# Impeccable session scratch (decision-round comps, question-server logs)
.impeccable/mocks/
.impeccable/questions/

# The web app is built in CI and uploaded straight to Cloudflare Pages.
# Committing it added ~6 MB per build, and it is not reproducible across
# machines, so the checked-in copy churned on every rebuild.
docs/app/
